import {Component} from '@angular/core';

@Component({
    selector:'home',
    templateUrl:'home.html',
    styleUrls: ['./css/home.css'],
})

export class home {
  titles = ["Luyện tập", "Kiểm tra", "Giải trí"]
}
